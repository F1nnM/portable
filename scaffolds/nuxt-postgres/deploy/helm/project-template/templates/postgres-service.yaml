{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "app.postgres.fullname" . }}
  labels:
    {{- include "app.postgres.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "app.postgres.selectorLabels" . | nindent 4 }}
{{- end }}
