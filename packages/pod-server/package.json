{
  "name": "@portable/pod-server",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "rm -rf dist && bun build src/index.ts --outdir dist --target node --sourcemap=linked --external @anthropic-ai/claude-agent-sdk",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.69",
    "@hono/node-server": "^1.14.0",
    "@hono/node-ws": "^1.1.0",
    "fdir": "^6.5.0",
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/ws": "^8.18.1",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  }
}
